# Copyright (c) Microsoft Corporation.
# Licensed under the MIT License.

$schema: http://azureml/sdk-2-0/CommandComponent.json
name: microsoft.com.azureml.samples.normalize_taxi_data
version: 0.0.1
display_name: Normalize Taxi Data
type: CommandComponent
tags: {}
inputs:
  filtered_data:
    type: DataFrameDirectory
    optional: false
outputs:
  output_normalize:
    type: DataFrameDirectory
command: >-
  python normalize.py --filtered_data {inputs.filtered_data} --output_normalize {outputs.output_normalize}
environment:
  docker:
    image: mcr.microsoft.com/azureml/intelmpi2018.3-ubuntu16.04
  conda:
    conda_dependencies:
      name: project_environment
      channels:
      - defaults
      - pandas
      - scikit-learn
      dependencies:
      - python=3.7.6
      - pip=20.0
      - pip:
        - azureml-sdk==0.1.0.*
        - azureml-designer-core==0.0.31
        - azureml-dataset-runtime[fuse,pandas]
        - --index-url https://azuremlsdktestpypi.azureedge.net/dev/aml/office/134157926D8F
        - --extra-index-url https://pypi.org/simple
        - pyarrow
  os: Linux
